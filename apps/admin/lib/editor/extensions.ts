// The editor ruleset now lives in @vetkit/db so the admin editor and the public
// renderer share one source of truth. Re-exported here to keep existing imports.
export { editorExtensions } from '@vetkit/db/editor';
