// @vetkit/db — the Supabase data layer (schema lives in ./supabase/migrations).
export * from './client';
export * from './schemas';
export type { Database, Tables, TablesInsert, TablesUpdate, Enums, Json } from './database.types';
